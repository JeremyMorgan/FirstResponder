@model IEnumerable<First_Responder.Areas.Admin.Models.RoleType>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.RoleTypeName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.RoleTypeName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.RoleTypeID }) |
            @Html.ActionLink("Details", "Details", new { id=item.RoleTypeID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.RoleTypeID })
        </td>
    </tr>
}

</table>
