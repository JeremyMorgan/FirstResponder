@model IEnumerable<First_Responder.Areas.Admin.Models.DriverType>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.DriverTypeName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.DriverTypeName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.DriverTypeID }) |
            @Html.ActionLink("Details", "Details", new { id=item.DriverTypeID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.DriverTypeID })
        </td>
    </tr>
}

</table>
